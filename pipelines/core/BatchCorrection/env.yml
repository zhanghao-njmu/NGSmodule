# Conda environment for the BatchCorrection pipeline.

name: ngsmodule-BatchCorrection
channels:
  - conda-forge
  - bioconda
dependencies:
  - r-base=4.3
  - bioconductor-sva=3.50
  - bioconductor-limma=3.58
  - jq
