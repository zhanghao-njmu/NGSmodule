# Conda environment for the postQuantificationQC pipeline.
# Bundled R script needs only base R; users can layer Bioconductor on top.

name: ngsmodule-postQuantificationQC
channels:
  - conda-forge
  - bioconda
dependencies:
  - r-base=4.3
  - jq
