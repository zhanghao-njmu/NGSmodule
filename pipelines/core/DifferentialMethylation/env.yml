# Conda environment for the DifferentialMethylation pipeline.
# Default engine is base R only; methylKit is opt-in via --dm_script.

name: ngsmodule-DifferentialMethylation
channels:
  - conda-forge
  - bioconda
dependencies:
  - r-base=4.3
  - jq
  # Optional: bioconductor-methylkit=1.28
